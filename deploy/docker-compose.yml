services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: inskill
      TZ: Asia/Shanghai
    ports: ["3306:3306"]
    volumes:
      - ../scripts/schema.sql:/docker-entrypoint-initdb.d/schema.sql:ro
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-proot"]
      interval: 5s
      retries: 20

  redis:
    image: redis:7
    ports: ["6379:6379"]

  rocketmq-namesrv:
    image: apache/rocketmq:5.1.4
    command: sh mqnamesrv
    ports: ["9876:9876"]

  rocketmq-broker:
    image: apache/rocketmq:5.1.4
    command: sh mqbroker -c /home/rocketmq/rocketmq-5.1.4/conf/broker.conf
    depends_on: ["rocketmq-namesrv"]
    environment:
      NAMESRV_ADDR: rocketmq-namesrv:9876
    ports: ["10909:10909", "10911:10911", "10912:10912"]
    volumes:
      - ./broker.conf:/home/rocketmq/rocketmq-5.1.4/conf/broker.conf:ro

  # 预创建 topic：RocketMQ consumer 启动要求 topic 路由存在
  rocketmq-init:
    image: apache/rocketmq:5.1.4
    depends_on: ["rocketmq-namesrv", "rocketmq-broker"]
    entrypoint: >
      sh -c "sleep 15 &&
      sh mqadmin updateTopic -n rocketmq-namesrv:9876 -c DefaultCluster -t seckill-order &&
      sh mqadmin updateTopic -n rocketmq-namesrv:9876 -c DefaultCluster -t stock-refund &&
      sh mqadmin updateTopic -n rocketmq-namesrv:9876 -c DefaultCluster -t cache-del &&
      echo topics created"
    restart: "no"

  app:
    build:
      context: ..
      dockerfile: Dockerfile
    environment:
      INSKILL_HTTP_ADDR: ":8081"
      INSKILL_MYSQL_DSN: "root:root@tcp(mysql:3306)/inskill?charset=utf8mb4&parseTime=True&loc=Local"
      INSKILL_REDIS_ADDR: "redis:6379"
      INSKILL_ROCKETMQ_NAMESRV: "rocketmq-namesrv:9876"
      INSKILL_UPLOAD_DIR: "/data/uploads"
    ports: ["8081:8081"]
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started
      rocketmq-broker:
        condition: service_started

volumes:
  mysql-data:
